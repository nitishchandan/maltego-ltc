from maltego_trx.decorator_registry import TransformRegistry, TransformSet

Helptransform_registry = TransformRegistry(
    owner="Nitish",
    author="Nitisdh",
    host_url="https://localhost:8080",
    seed_ids=["Helptransform"],
)
Helptransform_set = TransformSet("Helptransform", "Helptransform Transforms")
